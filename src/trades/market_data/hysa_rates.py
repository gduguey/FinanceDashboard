"""Not yet implemented.

Will hold: a fetched HYSA/risk-free rate time series — `rate(t)` for the
HYSA counterfactual (NEW_TASKS.md 2.2), following the same
cache-raw/derive-clean pattern as `cpi.py`. Until this is filled in,
`ledger.counterfactuals.hysa_counterfactual_value` takes a `rate_lookup`
callable that any caller can point at a constant rate or a real series.
"""
